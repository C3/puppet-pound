source 'https://rubygems.org'

group :test do
  gem "rake"
  gem "puppet", ENV['PUPPET_VERSION'] || '~> 3.4.0'
  gem "rspec-puppet"
  gem "puppetlabs_spec_helper"
  gem "puppet-syntax"
  gem "puppet-lint"
end
